cmake_minimum_required(VERSION 3.24)
project(ShinyLibraryManager VERSION 0.2.0 LANGUAGES C CXX)
include(CTest)
find_package(Python3 REQUIRED COMPONENTS Interpreter)
set(LIBRARY_ROOT "${CMAKE_CURRENT_SOURCE_DIR}")
set(LIBRARY_SCRIPTS "${LIBRARY_ROOT}/../../scripts")
set(LIBRARY_TESTS "${LIBRARY_ROOT}/../../tests/library-manager")
function(shiny_target target)
  target_compile_features(${target} PRIVATE cxx_std_20)
  target_include_directories(${target} PRIVATE "${LIBRARY_ROOT}")
  if(MSVC)
    target_compile_definitions(${target} PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN)
    target_compile_options(${target} PRIVATE /W4 /WX /permissive- /EHsc /utf-8)
    set_property(TARGET ${target} PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
  else()
    target_compile_options(${target} PRIVATE -Wall -Wextra -Wpedantic -Werror)
  endif()
endfunction()
if(WIN32)
  if(NOT EXISTS "${SHINY_SQLITE_SOURCE}/sqlite3.c")
    message(FATAL_ERROR "Run python scripts/fetch-library-deps.py and set SHINY_SQLITE_SOURCE to .deps/sqlite.")
  endif()
  add_library(shiny-sqlite STATIC "${SHINY_SQLITE_SOURCE}/sqlite3.c")
  target_include_directories(shiny-sqlite PUBLIC "${SHINY_SQLITE_SOURCE}")
  target_compile_definitions(shiny-sqlite PRIVATE SQLITE_THREADSAFE=1 SQLITE_DQS=0 SQLITE_DEFAULT_MEMSTATUS=0 SQLITE_OMIT_LOAD_EXTENSION SQLITE_ENABLE_API_ARMOR SQLITE_TRUSTED_SCHEMA=0)
  set_property(TARGET shiny-sqlite PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
else()
  find_package(SQLite3 3.38 REQUIRED)
  find_package(OpenSSL REQUIRED)
  add_library(shiny-sqlite INTERFACE)
  target_link_libraries(shiny-sqlite INTERFACE SQLite::SQLite3)
endif()
add_library(shiny-package-core STATIC platform.cpp package.cpp store.cpp)
shiny_target(shiny-package-core)
target_include_directories(shiny-package-core PUBLIC "${LIBRARY_ROOT}")
target_link_libraries(shiny-package-core PUBLIC shiny-sqlite)
if(WIN32)
  target_link_libraries(shiny-package-core PUBLIC bcrypt shell32 ole32 uuid)
else()
  target_link_libraries(shiny-package-core PUBLIC OpenSSL::Crypto)
endif()
add_executable(library-policy-tests "${LIBRARY_TESTS}/policy.cpp")
shiny_target(library-policy-tests)
add_test(NAME library-policy COMMAND library-policy-tests)
add_executable(library-store-tests "${LIBRARY_TESTS}/store.cpp")
shiny_target(library-store-tests)
target_link_libraries(library-store-tests PRIVATE shiny-package-core)
add_test(NAME library-store COMMAND library-store-tests)
add_executable(library-wire-tests "${LIBRARY_TESTS}/wire.cpp")
shiny_target(library-wire-tests)
add_test(NAME library-wire COMMAND library-wire-tests)
foreach(revision RANGE 1 3)
  add_executable(spatial-tests-${revision} "${LIBRARY_TESTS}/spatial.cpp" backends/spatial.cpp)
  shiny_target(spatial-tests-${revision})
  target_compile_definitions(spatial-tests-${revision} PRIVATE SHINY_SPATIAL_REVISION=${revision})
  target_link_libraries(spatial-tests-${revision} PRIVATE shiny-package-core)
  add_test(NAME spatial-reference-${revision} COMMAND spatial-tests-${revision})
endforeach()
add_executable(adaptive-detail-tests "${LIBRARY_TESTS}/adaptive.cpp" backends/spatial.cpp)
shiny_target(adaptive-detail-tests)
target_compile_definitions(adaptive-detail-tests PRIVATE SHINY_SPATIAL_REVISION=3)
target_link_libraries(adaptive-detail-tests PRIVATE shiny-package-core)
add_test(NAME adaptive-detail-quality COMMAND adaptive-detail-tests)
if(WIN32)
  add_executable(ShinyLibraryAudit main.cpp audit.cpp)
  shiny_target(ShinyLibraryAudit)
  target_link_libraries(ShinyLibraryAudit PRIVATE bcrypt version wintrust crypt32)
  add_library(library-load-canary SHARED "${LIBRARY_TESTS}/canary.cpp")
  shiny_target(library-load-canary)
  add_test(NAME library-audit-integration COMMAND ${Python3_EXECUTABLE} "${LIBRARY_TESTS}/integration.py" $<TARGET_FILE:ShinyLibraryAudit> $<TARGET_FILE:library-load-canary>)
  set_tests_properties(library-audit-integration PROPERTIES TIMEOUT 90)
  set(GENERATED "${CMAKE_CURRENT_BINARY_DIR}/generated/$<CONFIG>")
  set(PACKAGES "${CMAKE_BINARY_DIR}/$<CONFIG>/library-bundles")
  foreach(revision RANGE 1 3)
    add_library(shiny-spatial-${revision} SHARED backends/spatial.cpp)
    shiny_target(shiny-spatial-${revision})
    target_compile_definitions(shiny-spatial-${revision} PRIVATE SHINY_SPATIAL_REVISION=${revision})
  endforeach()
  add_custom_command(OUTPUT "${GENERATED}/bundle_catalog.hpp"
    COMMAND ${Python3_EXECUTABLE} "${LIBRARY_SCRIPTS}/library-bundles.py" "${GENERATED}/bundle_catalog.hpp" "${PACKAGES}" $<TARGET_FILE:shiny-spatial-1> $<TARGET_FILE:shiny-spatial-2> $<TARGET_FILE:shiny-spatial-3>
    DEPENDS shiny-spatial-1 shiny-spatial-2 shiny-spatial-3 "${LIBRARY_SCRIPTS}/library-bundles.py" VERBATIM)
  add_custom_target(library-bundles DEPENDS "${GENERATED}/bundle_catalog.hpp")
  add_executable(ShinyEnhancementWorker worker.cpp "${GENERATED}/bundle_catalog.hpp")
  shiny_target(ShinyEnhancementWorker)
  add_dependencies(ShinyEnhancementWorker library-bundles)
  target_include_directories(ShinyEnhancementWorker PRIVATE "${GENERATED}")
  target_link_libraries(ShinyEnhancementWorker PRIVATE shiny-package-core)
  set_target_properties(ShinyEnhancementWorker PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/$<CONFIG>")
  add_custom_command(OUTPUT "${GENERATED}/worker_identity.hpp"
    COMMAND ${Python3_EXECUTABLE} "${LIBRARY_SCRIPTS}/library-worker-identity.py" $<TARGET_FILE:ShinyEnhancementWorker> "${GENERATED}/worker_identity.hpp"
    DEPENDS ShinyEnhancementWorker "${LIBRARY_SCRIPTS}/library-worker-identity.py" VERBATIM)
  add_library(shiny-package-client STATIC client.cpp builtins.cpp "${GENERATED}/worker_identity.hpp" "${GENERATED}/bundle_catalog.hpp")
  shiny_target(shiny-package-client)
  target_include_directories(shiny-package-client PRIVATE "${GENERATED}")
  target_link_libraries(shiny-package-client PUBLIC shiny-package-core)
  add_executable(ShinyLibraryManagerCli cli.cpp)
  add_executable(ShinyLibraryManager WIN32 gui.cpp)
  foreach(target IN ITEMS ShinyLibraryManagerCli ShinyLibraryManager)
    shiny_target(${target})
    target_link_libraries(${target} PRIVATE shiny-package-client comctl32 comdlg32 dwmapi)
    set_target_properties(${target} PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/$<CONFIG>")
  endforeach()
  add_executable(library-fault-driver "${LIBRARY_TESTS}/fault-driver.cpp")
  shiny_target(library-fault-driver)
  target_link_libraries(library-fault-driver PRIVATE shiny-package-client)
  set_target_properties(library-fault-driver PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/$<CONFIG>")
  add_test(NAME library-manager-integration COMMAND ${Python3_EXECUTABLE} "${LIBRARY_TESTS}/manager.py" $<TARGET_FILE:ShinyLibraryManagerCli> $<TARGET_FILE:library-fault-driver> $<TARGET_FILE:ShinyLibraryManager>)
  set_tests_properties(library-manager-integration PROPERTIES TIMEOUT 180)
endif()
