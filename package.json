{
  "name": "shiny-engine",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Local-first media enhancement with a separately gated OpenDLSS-NR adapter. No proprietary models.",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test tests/unit/*.test.mjs",
    "check": "npm run typecheck && npm test && node scripts/audit.mjs",
    "test:browser": "python tests/browser/smoke.py && python tests/browser/neural.py && python tests/browser/neural-extension.py && python tests/browser/looklock.py && python tests/browser/player.py",
    "package": "npm run check && python scripts/package.py"
  },
  "devDependencies": { "typescript": "5.8.3" }
}
